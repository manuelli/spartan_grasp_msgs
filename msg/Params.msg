# if a field is empty that that means use the defaults

# should only be one of these
GraspParams[] grasp_params
Rectangle[] collision_objects # we only handle rectangles for now

# both of these should only have size one
Rectangle[] grasp_volume
Rectangle[] collision_volume


